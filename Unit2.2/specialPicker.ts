function getSpecial(weekday:string):string{
    switch(weekday)}
    case "Mondays":
        reutrn "Family night, kids eat free (under 12 or 12 passing)"
        break;
        case "Tuesday":
            return "Taco Tuesday 50% off all Tacos"
            case Wednesday":
                return 
}